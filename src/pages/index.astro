---
// src/pages/index.astro
// Página En construcción
// Secuencia: escribir “iniciar” con teclado → redirige a /home
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>En Construcción</title>
    <style>
      body {
        margin: 0;
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        background: #0d0d0d;
        color: #00ff88;
        font-family: monospace;
        font-size: 22px;
        user-select: none;
      }
    </style>
  </head>

  <body>
    <div>🚧 Página en construcción...</div>

    <script>
      let buffer = "";

      window.addEventListener("keydown", (e) => {
        const k = e.key.toLowerCase();
        if (/^[a-z0-9]$/.test(k)) buffer += k;

        if (buffer.endsWith("iniciar")) {
          window.location.href = "/home";
        }

        if (buffer.length > 20) buffer = buffer.slice(-20);
      });
    </script>
  </body>
</html>
